import { useEffect, useMemo, useState } from 'react'
import {
  ArrowRight, ArrowLeft, BookOpen, Building2, CheckCircle2, ChevronDown, ClipboardList, CreditCard, Eye, EyeOff, FileSpreadsheet, Home, LockKeyhole, LogOut, MapPin, Menu, Package, Search, ShoppingBag, ShoppingCart, Sparkles, UserRound, X, Minus, Plus, Trash2, ChevronLeft
} from 'lucide-react'
import type { PortalMode } from './types'
import { supabase } from './lib/supabase'
import StudentImport from './components/StudentImport'
import ParentCreate from './components/ParentCreate'
import AdminWorkspace from './AdminWorkspace'

type SchoolOption={id:string;name:string}
type BranchOption={id:string;name:string}
type CustomerPage='dashboard'|'packages'|'products'|'orders'|'profile'
type CartItem={id:string;title:string;type:'package'|'product';price:number;size?:string;quantity:number}
type CheckoutStep='cart'|'details'|'payment'|'success'

function App(){
  const [mode,setMode]=useState<PortalMode>('login')
  const [showLogin,setShowLogin]=useState(false)
  const [schools,setSchools]=useState<SchoolOption[]>([])
  const [branches,setBranches]=useState<BranchOption[]>([])
  const [school,setSchool]=useState('')
  const [branch,setBranch]=useState('')
  const [studentId,setStudentId]=useState('')
  const [password,setPassword]=useState('')
  const [showPassword,setShowPassword]=useState(false)
  const [loadingSchools,setLoadingSchools]=useState(true)
  const [loadingBranches,setLoadingBranches]=useState(false)
  const [loggingIn,setLoggingIn]=useState(false)
  const [error,setError]=useState('')
  const [customerPage,setCustomerPage]=useState<CustomerPage>('dashboard')

  useEffect(()=>{let cancelled=false;async function load(){if(!supabase){setError('Supabase is not configured.');setLoadingSchools(false);return}const {data,error}=await supabase.from('schools').select('id,name').eq('status','active').order('name');if(cancelled)return;if(error){console.error('School loading error:',error);setError(`Unable to load schools (${error.code??'unknown'}): ${error.message}`)}else setSchools(data??[]);setLoadingSchools(false)}void load();return()=>{cancelled=true}},[])
  const selectedSchoolName=useMemo(()=>schools.find(x=>x.id===school)?.name??'Your School',[schools,school])
  const selectedBranchName=useMemo(()=>branches.find(x=>x.id===branch)?.name??'Your Branch',[branches,branch])

  async function selectSchool(value:string){setSchool(value);setBranch('');setBranches([]);setStudentId('');setPassword('');setError('');if(!value||!supabase)return;setLoadingBranches(true);const {data,error}=await supabase.from('branches').select('id,name').eq('school_id',value).eq('status','active').order('name');if(error){console.error('Branch loading error:',error);setError(`Unable to load branches (${error.code??'unknown'}): ${error.message}`)}else{const list=data??[];setBranches(list);if(list.length===1)setBranch(list[0].id)}setLoadingBranches(false)}
  function selectBranch(value:string){setBranch(value);setStudentId('');setPassword('');setError('')}
  async function login(){if(!supabase||!school||!branch||!studentId.trim()||!password||loggingIn)return;setLoggingIn(true);setError('');const {data,error}=await supabase.functions.invoke('student-parent-login',{body:{school_id:school,branch_id:branch,login_id:studentId.trim(),password}});if(error||!data?.session){setError(data?.error??'Invalid school, branch, ID, or password.');setLoggingIn(false);return}const {error:se}=await supabase.auth.setSession({access_token:data.session.access_token,refresh_token:data.session.refresh_token});if(se){setError('Login succeeded, but the session could not be created.');setLoggingIn(false);return}setMode('store');setCustomerPage('dashboard');setShowLogin(false);setLoggingIn(false)}
  async function logout(){await supabase?.auth.signOut({scope:'local'});setMode('login');setStudentId('');setPassword('')}

  if(mode==='store')return <CustomerPortal schoolName={selectedSchoolName} branchName={selectedBranchName} studentId={studentId} page={customerPage} setPage={setCustomerPage} onLogout={()=>void logout()}/>
  if(mode==='admin')return <AdminPortal onBack={()=>setMode('login')}/>

  return <>
    <Landing onLogin={()=>setShowLogin(true)} />
    {showLogin&&<div className="login-overlay">
      <section className="login-card login-card-large">
        <button className="modal-close" onClick={()=>setShowLogin(false)} aria-label="Close"><X size={20}/></button>
        <div className="brand-mark"><img className="brand-mark-image" src="/favicon.png" alt="" /></div>
        <p className="eyebrow">SECURE SCHOOL PORTAL</p><h1>Welcome back</h1>
        <p className="subtitle">Select your school and branch, then sign in with the credentials provided by your school.</p>
        <label>School</label><div className="input-wrap select-wrap"><Building2 size={18}/><select value={school} disabled={loadingSchools} onChange={e=>void selectSchool(e.target.value)}><option value="">{loadingSchools?'Loading schools...':'Select your school'}</option>{schools.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><ChevronDown size={17}/></div>
        <label>Branch</label><div className="input-wrap select-wrap"><MapPin size={18}/><select value={branch} disabled={!school||loadingBranches} onChange={e=>selectBranch(e.target.value)}><option value="">{!school?'Select school first':loadingBranches?'Loading branches...':branches.length?'Select your branch':'No active branches'}</option>{branches.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select><ChevronDown size={17}/></div>
        <label>Student / Parent ID</label><div className="input-wrap"><UserRound size={18}/><input value={studentId} disabled={!branch} onChange={e=>setStudentId(e.target.value)} placeholder="Enter your ID" autoComplete="username"/></div>
        <label>Password</label><div className="input-wrap"><LockKeyhole size={18}/><input type={showPassword?'text':'password'} value={password} disabled={!branch} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void login()}} placeholder="Enter your password" autoComplete="current-password"/><button className="icon-button" onClick={()=>setShowPassword(v=>!v)} disabled={!branch}>{showPassword?<EyeOff size={18}/>:<Eye size={18}/>}</button></div>
        {error&&<p className="login-error">{error}</p>}
        <button className="primary-button" disabled={!school||!branch||!studentId.trim()||!password||loggingIn} onClick={()=>void login()}>{loggingIn?'SIGNING IN...':'SIGN IN TO PORTAL'}<ArrowRight size={18}/></button>
        <button className="text-button">Forgot Password?</button>
        <button className="admin-link" onClick={()=>{setShowLogin(false);setMode('admin')}}>Administrator Portal</button>
      </section>
    </div>}
  </>
}

const DEFAULT_HOME:any={hero:{slides:[{eyebrow:'PREMIUM SCHOOL UNIFORMS',title:['Dress for','Brighter','Tomorrows.'],text:'Quality uniforms for confident learners. Shop school-approved uniforms, packages and accessories — all in one place.',image:'/hero-slide-1.jpg',button:'Shop Uniforms',secondary:'View Packages',enabled:true},{eyebrow:'SCHOOL UNIFORMS',title:['Confidence','Looks Good','On You'],text:'Official school uniforms, curated packages and quality accessories — designed for every step of their journey.',image:'/hero-slide-2.jpg',button:'Shop Uniforms',secondary:'View Packages',enabled:true},{eyebrow:'UNIFORMS THAT INSPIRE',title:['Quality Today','for Greater','Tomorrow'],text:'Premium school uniforms crafted with care, comfort and confidence for every school day.',image:'/hero-slide-3.jpg',button:'Shop Uniforms',secondary:'View Packages',enabled:true}]},trust:[{title:'School Approved',text:'Official uniforms',enabled:true},{title:'Premium Quality',text:'Comfortable & Durable',enabled:true},{title:'Easy Online Ordering',text:'Hassle free shopping',enabled:true},{title:'Fast & Reliable Delivery',text:'To your doorstep',enabled:true}],categories:[{title:'Boys Uniforms',image:'/category-boys.jpg',target:'uniforms',enabled:true},{title:'Girls Uniforms',image:'/category-girls.jpg',target:'uniforms',enabled:true},{title:'Packages',image:'/category-packages.jpg',target:'packages',enabled:true},{title:'Accessories',image:'/category-accessories.jpg',target:'accessories',enabled:true}],featured:[{title:'Boys Uniforms',text:'Premium quality & comfort for every school day.',image:'/category-boys.jpg',target:'uniforms',enabled:true},{title:'Girls Uniforms',text:'Premium quality & comfort for every school day.',image:'/category-girls.jpg',target:'uniforms',enabled:true},{title:'Complete Packages',text:'Everything your child needs in one set.',image:'/category-packages.jpg',target:'packages',enabled:true}],afterFold:{eyebrow:'MADE FOR EVERY SCHOOL DAY',title:['Uniforms that look right.','Feel right too.'],text:'From the first day of term to the final school bell, Artisan makes it easier to get the right uniform, the right size and the right school-approved products.',button:'Explore the Store',image:'/hero-slide-3.jpg',enabled:true},benefits:[{title:'School Approved',text:'Official products',enabled:true},{title:'Comfortable Fit',text:'Made for everyday wear',enabled:true},{title:'Complete Packages',text:'Save time & effort',enabled:true},{title:'Trusted Shopping',text:'Simple online ordering',enabled:true}],cta:{eyebrow:'READY FOR THE NEW TERM?',title:['Everything your student needs.','One simple place.'],button:'Enter Your School Store',enabled:true},footer:{tagline:'School uniforms, made simple.',shopTitle:'Shop',helpTitle:'Help',informationTitle:'Information',copyright:'© 2026 Artisan. All rights reserved.',credit:'Developed by Dexorzo Creations.',secondary:'School-specific shopping • Secure access'}}

function Landing({onLogin}:{onLogin:()=>void}){
  const [publicPage,setPublicPage]=useState('home')
  const go=(p:string)=>{setPublicPage(p);setMenu(false);window.scrollTo({top:0,behavior:'smooth'})}

  const [menu,setMenu]=useState(false),[slide,setSlide]=useState(0),[cartCount]=useState(0),[home,setHome]=useState<any>(DEFAULT_HOME),[searchOpen,setSearchOpen]=useState(false),[search,setSearch]=useState('')
  useEffect(()=>{if(!supabase)return;void (supabase as any).from('homepage_content').select('content').eq('slug','default').eq('is_published',true).maybeSingle().then(({data}:{data:any})=>{if(data?.content)setHome({...DEFAULT_HOME,...data.content})})},[])
  const slides=(home.hero?.slides??DEFAULT_HOME.hero.slides).filter((item:any)=>item.enabled!==false).map((item:any,i:number)=>({...item,className:i===1?'hero-navy':i===2?'hero-cream':'hero-light'}))
  useEffect(()=>{const count=slides.length; if(!count)return; const t=window.setInterval(()=>setSlide(s=>(s+1)%count),6000);return()=>window.clearInterval(t)},[slides.length])
  if(publicPage!=='home') return <PublicPage page={publicPage} onLogin={onLogin} onNavigate={go}/>
  const current=slides[Math.min(slide,Math.max(0,slides.length-1))]
  const next=()=>setSlide(s=>(s+1)%slides.length),prev=()=>setSlide(s=>(s+slides.length-1)%slides.length)
  return <main className="landing exact-home">
    <header className="store-header exact-header"><div className="store-header-inner">
      <button className="brand-button" onClick={()=>go('home')} aria-label="Go to home"><img className="brand-logo" src="/logo.png" alt="Artisan" /></button>
      <nav className={menu?'nav-open':''}><a className="active" onClick={()=>go('home')}>Home</a><a onClick={()=>go('uniforms')}>Uniforms</a><a onClick={()=>go('packages')}>Packages</a><a onClick={()=>go('accessories')}>Accessories</a><a onClick={()=>go('size-guide')}>Size Guide</a><a onClick={()=>go('orders')}>Orders</a><a onClick={()=>go('help')}>Help</a></nav>
      <div className="exact-header-icons"><button aria-label="Search" onClick={()=>setSearchOpen(v=>!v)}><Search/></button><button aria-label="Account" onClick={onLogin}><UserRound/></button><button className="mini-cart" aria-label="Cart" onClick={onLogin}><ShoppingCart/><b>{cartCount}</b></button></div>
      {searchOpen&&<div className="header-search"><Search size={17}/><input autoFocus value={search} onChange={e=>setSearch(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&search.trim()){go('uniforms');setSearchOpen(false)}}} placeholder="Search uniforms, packages & accessories"/><button onClick={()=>setSearchOpen(false)}><X size={16}/></button></div>}<button className="mobile-menu" onClick={()=>setMenu(v=>!v)}>{menu?<X/>:<Menu/>}</button>
    </div></header>

    <section className={`exact-hero ${current.className}`}>
      <div className="exact-hero-copy"><p className="eyebrow">{current.eyebrow}</p><h1 key={slide}>{current.title.map((x:string,i:number)=><span key={i}>{x}{i<current.title.length-1&&<br/>}</span>)}</h1><p>{current.text}</p><div className="exact-hero-actions"><button onClick={onLogin}>{current.button} <ArrowRight size={16}/></button><a href="#packages">{current.secondary}</a></div><div className="exact-slider-controls"><button onClick={prev} aria-label="Previous"><ChevronLeft/></button><div>{slides.map((_,i)=><button key={i} className={i===slide?'active':''} onClick={()=>setSlide(i)} aria-label={"Slide "+(i+1)}/>)}</div><button onClick={next} aria-label="Next"><ArrowRight/></button><span>0{slide+1} / 0{slides.length}</span></div></div>
      <div className="exact-hero-image"><img key={slide} src={current.image} alt="Students wearing school uniforms"/></div>
    </section>

    <section className="exact-trust">{home.trust.filter((x:any)=>x.enabled!==false).map((x:any,i:number)=><TrustExact key={i} icon={[<Building2/>,<Sparkles/>,<ShoppingBag/>,<Package/>][i%4]} title={x.title} text={x.text}/>)}</section>

    <section id="collections" className="exact-section"><div className="exact-section-title"><h2>Shop by Category</h2><button onClick={()=>go('uniforms')}>View All <ArrowRight/></button></div><div className="exact-category-grid">{home.categories.filter((x:any)=>x.enabled!==false).map((x:any,i:number)=><ExactCategory key={i} image={x.image} title={x.title} onClick={()=>go(x.target||'uniforms')}/>)}</div></section>

    <section id="packages" className="exact-section exact-featured"><div className="exact-section-title"><h2>Featured Collections</h2><button onClick={()=>go('uniforms')}>View All <ArrowRight/></button></div><div className="exact-feature-grid">{home.featured.filter((x:any)=>x.enabled!==false).map((x:any,i:number)=><ExactFeature key={i} image={x.image} title={x.title} text={x.text} onClick={()=>go(x.target||'uniforms')}/>)}</div></section>

    {home.afterFold?.enabled!==false&&<section className="exact-after-fold"><div><p className="eyebrow">{home.afterFold.eyebrow}</p><h2>{home.afterFold.title.map((x:string,i:number)=><span key={i}>{x}{i<home.afterFold.title.length-1&&<br/>}</span>)}</h2><p>{home.afterFold.text}</p><button onClick={onLogin}>{home.afterFold.button} <ArrowRight/></button></div><img src={home.afterFold.image} alt="Artisan school uniforms"/></section>}
    <section id="accessories" className="exact-benefits">{home.benefits.filter((x:any)=>x.enabled!==false).map((x:any,i:number)=><div key={i}><strong>{x.title}</strong><span>{x.text}</span></div>)}</section>
    {home.cta?.enabled!==false&&<section id="support" className="exact-cta"><div><p className="eyebrow">{home.cta.eyebrow}</p><h2>{home.cta.title.map((x:string,i:number)=><span key={i}>{x}{i<home.cta.title.length-1&&<br/>}</span>)}</h2></div><button onClick={onLogin}>{home.cta.button} <ArrowRight/></button></section>}
    <footer className="store-footer exact-footer"><div className="footer-brand"><img className="brand-logo" src="/logo.png" alt="Artisan"/><p>{home.footer.tagline}</p></div><div className="footer-nav"><div><strong>Shop</strong><a onClick={()=>go('uniforms')}>Uniforms</a><a onClick={()=>go('packages')}>Packages</a><a onClick={()=>go('accessories')}>Accessories</a></div><div><strong>Help</strong><a onClick={()=>go('size-guide')}>Size Guide</a><a onClick={onLogin}>Parent / Student Login</a><a onClick={()=>go('contact')}>Contact Us</a></div><div><strong>Information</strong><a onClick={()=>go('about')}>About Artisan</a><a onClick={()=>go('terms')}>Terms & Conditions</a><a onClick={()=>go('privacy')}>Privacy Policy</a></div></div><div className="footer-bottom"><span>{home.footer.copyright} • {home.footer.credit}</span><span>{home.footer.secondary}</span></div></footer>
  </main>
}

function PublicPage({page,onLogin,onNavigate}:{page:string;onLogin:()=>void;onNavigate:(p:string)=>void}){
  const info:{[key:string]:{eyebrow:string;title:string;text:string}}={
    uniforms:{eyebrow:'SHOP UNIFORMS',title:'School uniforms, selected for your school.',text:'Browse boys and girls uniform collections after signing in. Your school controls the available products, sizes and pricing.'},
    packages:{eyebrow:'COMPLETE SETS',title:'Uniform packages made simple.',text:'Choose a complete school-ready package assembled from approved individual products.'},
    accessories:{eyebrow:'THE DETAILS MATTER',title:'Accessories for every school day.',text:'Find approved shoes, belts, ties, socks and other essentials alongside your uniform collection.'},
    'size-guide':{eyebrow:'FIT & SIZING',title:'Find the right fit.',text:'Size requirements can differ by school and product. Your authenticated school catalogue shows the applicable sizes.'},
    orders:{eyebrow:'YOUR ORDERS',title:'Your orders, all in one place.',text:'Sign in to view orders, delivery information and purchase history for your selected school and branch.'},
    help:{eyebrow:'HELP CENTER',title:'Need a hand?',text:'Find answers about school access, ordering, sizes, delivery and account support.'},
    about:{eyebrow:'ABOUT ARTISAN',title:'School uniforms, made simple.',text:'Artisan gives schools, parents and students one organized place for school-specific uniform shopping.'},
    terms:{eyebrow:'LEGAL',title:'Terms & Conditions',text:'These terms describe general use of the Artisan school uniform storefront. School-specific ordering conditions may also apply.'},
    privacy:{eyebrow:'LEGAL',title:'Privacy Policy',text:'Artisan uses authenticated access to provide school-specific catalogues, accounts and ordering services.'},
    contact:{eyebrow:'CONTACT US',title:'We are here to help.',text:'Questions about your school store, an order, sizing or access? Send us a message and our support team can help.'}
  }
  const d=info[page]??info.help
  return <main className="public-page"><header className="store-header exact-header"><div className="store-header-inner"><button className="brand-button" onClick={()=>onNavigate('home')}><img className="brand-logo" src="/logo.png" alt="Artisan"/></button><nav><a onClick={()=>onNavigate('home')}>Home</a><a onClick={()=>onNavigate('uniforms')}>Uniforms</a><a onClick={()=>onNavigate('packages')}>Packages</a><a onClick={()=>onNavigate('accessories')}>Accessories</a><a onClick={()=>onNavigate('size-guide')}>Size Guide</a><a onClick={()=>onNavigate('orders')}>Orders</a><a onClick={()=>onNavigate('help')}>Help</a></nav><div className="exact-header-icons"><button onClick={onLogin}><UserRound/></button><button className="mini-cart" onClick={onLogin}><ShoppingCart/><b>0</b></button></div></div></header>
  <section className="public-hero"><div><p className="eyebrow">{d.eyebrow}</p><h1>{d.title}</h1><p>{d.text}</p>{['uniforms','packages','accessories','orders'].includes(page)&&<button onClick={onLogin}>Enter Your School Store <ArrowRight/></button>}</div>{['uniforms','packages','accessories'].includes(page)&&<div className="public-card-grid">{(page==='uniforms'?[['/category-boys.jpg','Boys Uniforms'],['/category-girls.jpg','Girls Uniforms']]:page==='packages'?[['/category-packages.jpg','Complete Packages']]:[['/category-accessories.jpg','Accessories']]).map(([img,title])=><button key={title} onClick={onLogin}><img src={img} alt=""/><strong>{title}</strong><span>Sign in to shop <ArrowRight/></span></button>)}</div>}</section>
  {page==='contact'?<ContactContent/>:page==='help'?<HelpContent onNavigate={onNavigate}/>:page==='size-guide'?<SizeContent/>:page==='about'?<InfoContent title="About Artisan" text={d.text}/>:page==='terms'?<InfoContent title="Terms & Conditions" text={d.text}/>:page==='privacy'?<InfoContent title="Privacy Policy" text={d.text}/>:<div className="public-bottom-note"><p>{page==='orders'?'Your order history is protected inside your school portal.':'Secure school-specific shopping'}</p><button onClick={onLogin}>Parent / Student Login <ArrowRight/></button></div>}
  <footer className="store-footer exact-footer"><div className="footer-brand"><button className="brand-button" onClick={()=>onNavigate('home')}><img className="brand-logo" src="/logo.png" alt="Artisan"/></button><p>School uniforms, made simple.</p></div><div className="footer-nav"><div><strong>Shop</strong><a onClick={()=>onNavigate('uniforms')}>Uniforms</a><a onClick={()=>onNavigate('packages')}>Packages</a><a onClick={()=>onNavigate('accessories')}>Accessories</a></div><div><strong>Help</strong><a onClick={()=>onNavigate('size-guide')}>Size Guide</a><a onClick={onLogin}>Parent / Student Login</a><a onClick={()=>onNavigate('contact')}>Contact Us</a></div><div><strong>Information</strong><a onClick={()=>onNavigate('about')}>About Artisan</a><a onClick={()=>onNavigate('terms')}>Terms & Conditions</a><a onClick={()=>onNavigate('privacy')}>Privacy Policy</a></div></div><div className="footer-bottom"><span>© 2026 Artisan. All rights reserved.</span><span>School-specific shopping • Secure access</span></div></footer></main>
}
function ContactContent(){const [sent,setSent]=useState(false);return <section className="public-content contact-layout"><div><p className="eyebrow">GET IN TOUCH</p><h2>Contact Artisan</h2><p>For school store access, orders, sizing or delivery questions, send us a message.</p><div className="contact-details"><span><Building2/> School store support</span><span><ShoppingBag/> Order & product help</span><span><MapPin/> School and branch assistance</span></div></div><form onSubmit={e=>{e.preventDefault();setSent(true)}}><label>Name<input required/></label><label>Email<input type="email" required/></label><label>School / Branch<input/></label><label>Message<textarea required rows={5}/></label><button type="submit">{sent?'MESSAGE SENT':'SEND MESSAGE'} <ArrowRight/></button>{sent&&<p className="form-success">Thanks. Your message has been captured for support follow-up.</p>}</form></section>}
function HelpContent({onNavigate}:{onNavigate:(p:string)=>void}){return <section className="public-content faq-grid"><div><p className="eyebrow">HELP CENTER</p><h2>Common questions</h2></div><div>{[['How do I shop for my school?','Select Parent / Student Login and sign in with the credentials provided by your school.'],['Why can’t I see another school’s products?','The storefront is school-specific and your authenticated account determines the catalogue.'],['Where can I get sizing help?','Use the Size Guide and follow the measurements provided by your school catalogue.'],['Need more help?','Use Contact Us and send the school or branch details with your question.']].map(([q,a])=><article key={q}><h3>{q}</h3><p>{a}</p></article>)}<button onClick={()=>onNavigate('contact')}>Contact Us <ArrowRight/></button></div></section>}
function SizeContent(){return <section className="public-content size-content"><div><p className="eyebrow">SIZE GUIDE</p><h2>Choose the right size with confidence.</h2><p>Once you sign in, the store can show the sizes configured for your school and branch.</p></div><div className="size-table">{[['Step 1','Open your school store'],['Step 2','Select the product'],['Step 3','Review available sizes'],['Step 4','Choose and add to cart']].map(([a,b])=><div key={a}><strong>{a}</strong><span>{b}</span></div>)}</div></section>}
function InfoContent({title,text}:{title:string;text:string}){return <section className="public-content legal-content"><p className="eyebrow">ARTISAN</p><h2>{title}</h2><p>{text}</p><div><h3>School-specific information</h3><p>Where an order, catalogue, payment, delivery or account rule is specific to a school or branch, the applicable information shown inside the authenticated school store applies to that service.</p></div></section>}
function TrustExact({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <div className="exact-trust-item"><div>{icon}</div><span><strong>{title}</strong><small>{text}</small></span></div>}
function ExactCategory({image,title,onClick}:{image:string;title:string;onClick:()=>void}){return <button className="exact-category" onClick={onClick}><img src={image} alt=""/><div><strong>{title}</strong><span>Shop Now <ArrowRight/></span></div></button>}
function ExactFeature({image,title,text,onClick}:{image:string;title:string;text:string;onClick:()=>void}){return <button className="exact-feature" onClick={onClick}><img src={image} alt=""/><div><strong>{title}</strong><span>{text}</span><em>Shop Collection <ArrowRight/></em></div></button>}
function Feature({icon,title,text}:{icon:React.ReactNode;title:string;text:string}){return <div className="feature"><div className="feature-icon">{icon}</div><div><strong>{title}</strong><p>{text}</p></div></div>}
function Step({n,title,text}:{n:string;title:string;text:string}){return <div className="step"><span>{n}</span><h3>{title}</h3><p>{text}</p></div>}

function CustomerPortal({schoolName,branchName,studentId,page,setPage,onLogout}:{schoolName:string;branchName:string;studentId:string;page:CustomerPage;setPage:(p:CustomerPage)=>void;onLogout:()=>void}){
  const [cart,setCart]=useState<CartItem[]>([]),[checkout,setCheckout]=useState<CheckoutStep|null>(null),[selected,setSelected]=useState<CartItem|null>(null)
  const add=(item:CartItem)=>setCart(items=>{const f=items.find(x=>x.id===item.id&&x.size===item.size);return f?items.map(x=>x.id===item.id&&x.size===item.size?{...x,quantity:x.quantity+item.quantity}:x):[...items,item]})
  const update=(id:string,d:number)=>setCart(items=>items.map(x=>x.id===id?{...x,quantity:Math.max(1,x.quantity+d)}:x))
  const remove=(id:string)=>setCart(items=>items.filter(x=>x.id!==id))
  const total=cart.reduce((s,x)=>s+x.price*x.quantity,0)
  if(checkout)return <CheckoutFlow cart={cart} total={total} step={checkout} setStep={setCheckout} update={update} remove={remove} onComplete={()=>{setCart([]);setCheckout('success')}}/>
  if(selected)return <ProductDetail item={selected} onBack={()=>setSelected(null)} onAdd={x=>{add(x);setSelected(null)}}/>
  const nav=[['dashboard','Dashboard',Home],['packages','Uniform Packages',Package],['products','Individual Products',ShoppingBag],['orders','My Orders',ClipboardList],['profile','Profile',UserRound]] as const
  return <div className="portal"><aside className="sidebar"><div className="portal-brand"><img className="brand-logo" src="/logo.png" alt="Artisan" /><div><span>Parent Portal</span></div></div><div className="school-scope"><Building2 size={16}/><div><strong>{schoolName}</strong><span>{branchName}</span></div></div><nav>{nav.map(([key,label,Icon])=><button className={page===key?'active':''} onClick={()=>setPage(key)} key={key}><Icon size={18}/>{label}</button>)}</nav><button className="sidebar-logout" onClick={onLogout}><LogOut size={17}/> Logout</button></aside><main className="portal-main"><header className="portal-header"><div><p className="eyebrow">{branchName}</p><h1>{page==='dashboard'?'Good morning':page==='packages'?'Uniform Packages':page==='products'?'Individual Products':page==='orders'?'My Orders':'My Profile'}{page==='dashboard'&&<span>, {studentId}</span>}</h1></div><div className="portal-header-actions"><button className="cart-button" onClick={()=>setCheckout('cart')}><ShoppingCart size={19}/><span>Cart</span>{cart.length>0&&<b>{cart.reduce((s,x)=>s+x.quantity,0)}</b>}</button><div className="header-user"><div className="avatar">{studentId.slice(0,1).toUpperCase()}</div><div><strong>{studentId}</strong><span>Parent / Student</span></div></div></div></header>{page==='dashboard'?<Dashboard setPage={setPage}/>:page==='packages'?<Packages onView={setSelected} onAdd={add}/>:page==='products'?<Products onView={setSelected} onAdd={add}/>:page==='orders'?<Orders/>:<Profile studentId={studentId}/>}</main></div>
}
function Dashboard({setPage}:{setPage:(p:CustomerPage)=>void}){return <div className="portal-content"><div className="welcome-banner"><div><p className="eyebrow">YOUR SCHOOL STORE</p><h2>Uniform shopping, organized for you.</h2><p>Choose a complete package for the term or replace individual items as needed.</p><button className="hero-primary" onClick={()=>setPage('packages')}>Explore Uniform Packages <ArrowRight size={17}/></button></div><div className="banner-icon"><ShoppingBag size={58}/></div></div><div className="portal-grid"><Stat title="Active orders" value="0" icon={<ClipboardList/>}/><Stat title="Saved items" value="0" icon={<Package/>}/><Stat title="School branch" value="Active" icon={<Building2/>}/></div><div className="section-row"><p className="eyebrow">SHOP</p><h2>Start with what you need</h2></div><div className="shop-cards"><ShopCard icon={<Package/>} title="Uniform Packages" text="Complete boys and girls packages assembled from approved school products." action="View packages" onClick={()=>setPage('packages')}/><ShopCard icon={<ShoppingBag/>} title="Individual Products" text="Buy or replace individual shirts, pants, shoes, socks, belts and more." action="Browse products" onClick={()=>setPage('products')}/></div></div>}
function Stat({title,value,icon}:{title:string;value:string;icon:React.ReactNode}){return <div className="stat"><div>{icon}</div><span>{title}</span><strong>{value}</strong></div>}
function ShopCard({icon,title,text,action,onClick}:{icon:React.ReactNode;title:string;text:string;action:string;onClick:()=>void}){return <div className="shop-card"><div className="feature-icon">{icon}</div><h3>{title}</h3><p>{text}</p><button onClick={onClick}>{action}<ArrowRight size={16}/></button></div>}
function Packages({onView,onAdd}:{onView:(x:CartItem)=>void;onAdd:(x:CartItem)=>void}){const items=[{id:'boys-package',title:'Boys Uniform Package',type:'package' as const,price:4999,text:'Complete daily uniform essentials',badge:'BOYS'},{id:'girls-package',title:'Girls Uniform Package',type:'package' as const,price:4999,text:'Complete daily uniform essentials',badge:'GIRLS'},{id:'sports-package',title:'Sports Uniform Package',type:'package' as const,price:2499,text:'Sports and activity essentials',badge:'SPORTS'}];return <div className="portal-content"><CatalogHeading eyebrow="PACKAGES" title="Uniform Packages" text="Complete school-approved sets, built from the same individual products available in the store."/><div className="catalog-grid">{items.map(x=><ProductCard key={x.id} {...x} onView={()=>onView({...x,quantity:1})} onAdd={()=>onAdd({...x,quantity:1})}/>)}</div></div>}
function Products({onView,onAdd}:{onView:(x:CartItem)=>void;onAdd:(x:CartItem)=>void}){const items=[['shirt','School Shirt','Approved daily uniform shirt',799],['trousers','School Trousers','Approved daily uniform trousers',1099],['belt','School Belt','Approved uniform belt',299],['tie','School Tie','Approved school tie',249],['socks','School Socks','Approved uniform socks',199],['shoes','School Shoes','Approved black school shoes',1599]].map(([id,title,text,price])=>({id:id as string,title:title as string,text:text as string,price:price as number,type:'product' as const}));return <div className="portal-content"><CatalogHeading eyebrow="PRODUCTS" title="Individual Products" text="Find a single item, select the right size and add it to your order."/><div className="catalog-toolbar"><div className="search-box"><Search size={17}/><input placeholder="Search products"/></div><select><option>All categories</option><option>Shirts</option><option>Pants</option><option>Shoes</option><option>Accessories</option></select></div><div className="catalog-grid">{items.map(x=><ProductCard key={x.id} {...x} onView={()=>onView({...x,quantity:1})} onAdd={()=>onAdd({...x,quantity:1})}/>)}</div></div>}
function ProductCard({badge,title,text,price,onView,onAdd,id,type}:{badge?:string;title:string;text:string;price:number;onView:()=>void;onAdd:()=>void;id:string;type:'package'|'product'}){const visual=id.includes('girls')||title.toLowerCase().includes('girls')?'/girls-uniform.svg':id.includes('boys')||title.toLowerCase().includes('boys')?'/boys-uniform.svg':'/accessories.svg';return <article className="product-card"><button className="product-image product-image-button" onClick={onView}><img src={visual} alt="" />{badge&&<span>{badge}</span>}<i className="quick-view">View</i></button><div className="product-copy"><p>{text}</p><h3>{title}</h3><div className="price-row"><strong>₹{price.toLocaleString('en-IN')}</strong><span>School approved</span></div><div className="product-actions"><button onClick={onView}>View details</button><button className="add-button" onClick={onAdd}>Add to cart <ArrowRight size={15}/></button></div></div></article>}
function CatalogHeading({eyebrow,title,text}:{eyebrow:string;title:string;text:string}){return <div className="section-heading portal-heading"><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p>{text}</p></div>}
function ProductDetail({item,onBack,onAdd}:{item:CartItem;onBack:()=>void;onAdd:(x:CartItem)=>void}){const [size,setSize]=useState(''),[quantity,setQuantity]=useState(1);return <div className="checkout-page"><div className="checkout-top"><button onClick={onBack}><ChevronLeft size={18}/> Back to store</button><strong>Product Details</strong></div><div className="detail-card"><div className="detail-image"><img src={item.title.toLowerCase().includes('girls')?'/girls-uniform.svg':item.title.toLowerCase().includes('boys')?'/boys-uniform.svg':'/accessories.svg'} alt="" /></div><div className="detail-copy"><p className="eyebrow">{item.type==='package'?'UNIFORM PACKAGE':'INDIVIDUAL PRODUCT'}</p><h1>{item.title}</h1><p>School-approved product for your selected school and branch. Final availability and pricing are controlled by the school catalog.</p><strong className="detail-price">₹{item.price.toLocaleString('en-IN')}</strong><label>Size<select value={size} onChange={e=>setSize(e.target.value)}><option value="">Select size</option>{['XS','S','M','L','XL','XXL'].map(x=><option key={x}>{x}</option>)}</select></label><div className="quantity"><span>Quantity</span><button onClick={()=>setQuantity(Math.max(1,quantity-1))}><Minus/></button><b>{quantity}</b><button onClick={()=>setQuantity(quantity+1)}><Plus/></button></div><button className="primary-button" disabled={!size} onClick={()=>onAdd({...item,size,quantity})}>Add to Cart <ShoppingCart size={18}/></button></div></div></div>}
function CheckoutFlow({cart,total,step,setStep,update,remove,onComplete}:{cart:CartItem[];total:number;step:CheckoutStep;setStep:(x:CheckoutStep|null)=>void;update:(id:string,d:number)=>void;remove:(id:string)=>void;onComplete:()=>void}){const [address,setAddress]=useState({name:'',phone:'',line1:'',city:'',state:'Telangana',pincode:''});if(step==='success')return <div className="checkout-page"><div className="success-card"><div className="success-icon"><CheckCircle2 size={48}/></div><p className="eyebrow">ORDER CONFIRMED</p><h1>Thank you for your order.</h1><p>Your order has been recorded for the selected school branch.</p><strong>Order #SU-2026-0001</strong><button className="primary-button" onClick={()=>setStep(null)}>Back to Store</button></div></div>;if(step==='cart')return <div className="checkout-page"><div className="checkout-top"><button onClick={()=>setStep(null)}><ChevronLeft/> Continue shopping</button><strong>Your Cart</strong></div><div className="checkout-layout"><div className="cart-card"><h1>Review your items</h1>{cart.length===0?<div className="empty-state"><ShoppingCart size={40}/><h3>Your cart is empty</h3></div>:cart.map(x=><div className="cart-row" key={x.id}><div className="cart-thumb"><ShoppingBag/></div><div><strong>{x.title}</strong><span>{x.size?('Size '+x.size+' • '):''}₹{x.price.toLocaleString('en-IN')} each</span><div className="qty-controls"><button onClick={()=>update(x.id,-1)}><Minus/></button><b>{x.quantity}</b><button onClick={()=>update(x.id,1)}><Plus/></button></div></div><strong>₹{(x.price*x.quantity).toLocaleString('en-IN')}</strong><button className="remove-button" onClick={()=>remove(x.id)}><Trash2 size={17}/></button></div>)}</div><OrderSummary total={total} disabled={!cart.length} onNext={()=>setStep('details')}/></div></div>;if(step==='details')return <CheckoutDetails address={address} setAddress={setAddress} onBack={()=>setStep('cart')} onNext={()=>setStep('payment')}/>;return <PaymentStep total={total} onBack={()=>setStep('details')} onPay={onComplete}/>}
function OrderSummary({total,disabled,onNext}:{total:number;disabled:boolean;onNext:()=>void}){return <aside className="order-summary"><h3>Order Summary</h3><div><span>Subtotal</span><strong>₹{total.toLocaleString('en-IN')}</strong></div><div><span>Delivery</span><strong>Calculated at checkout</strong></div><div className="summary-total"><span>Total</span><strong>₹{total.toLocaleString('en-IN')}</strong></div><button className="primary-button" disabled={disabled} onClick={onNext}>Checkout <ArrowRight size={17}/></button></aside>}
function CheckoutDetails({address,setAddress,onBack,onNext}:{address:{name:string;phone:string;line1:string;city:string;state:string;pincode:string};setAddress:(x:any)=>void;onBack:()=>void;onNext:()=>void}){const ok=Object.values(address).every(Boolean);return <div className="checkout-page"><div className="checkout-top"><button onClick={onBack}><ChevronLeft/> Cart</button><strong>Delivery Details</strong></div><div className="checkout-form-card"><p className="eyebrow">STEP 1 OF 2</p><h1>Where should we deliver?</h1><div className="checkout-form-grid">{[['name','Full name'],['phone','Phone'],['line1','Address'],['city','City'],['state','State'],['pincode','PIN code']].map(([k,label])=><label key={k}>{label}<input value={(address as any)[k]} onChange={e=>setAddress({...address,[k]:e.target.value})}/></label>)}</div><button className="primary-button" disabled={!ok} onClick={onNext}>Continue to Payment <ArrowRight/></button></div></div>}
function PaymentStep({total,onBack,onPay}:{total:number;onBack:()=>void;onPay:()=>void}){const [method,setMethod]=useState('online');return <div className="checkout-page"><div className="checkout-top"><button onClick={onBack}><ChevronLeft/> Delivery</button><strong>Payment</strong></div><div className="payment-card"><p className="eyebrow">STEP 2 OF 2</p><h1>Choose payment method</h1><label className="payment-option"><input type="radio" checked={method==='online'} onChange={()=>setMethod('online')}/><CreditCard/><div><strong>Online Payment</strong><span>Secure payment gateway</span></div></label><label className="payment-option"><input type="radio" checked={method==='cod'} onChange={()=>setMethod('cod')}/><ShoppingBag/><div><strong>Pay at School</strong><span>Available if enabled by your school</span></div></label><div className="payment-total"><span>Payable</span><strong>₹{total.toLocaleString('en-IN')}</strong></div><button className="primary-button" onClick={onPay}>Place Order <CheckCircle2 size={18}/></button></div></div>}
function Orders(){return <div className="portal-content"><CatalogHeading eyebrow="ORDERS" title="My Orders" text="Track your school uniform orders and view previous purchases."/><div className="empty-state"><ClipboardList size={42}/><h3>No orders yet</h3><p>Your completed orders will appear here.</p></div></div>}
function Profile({studentId}:{studentId:string}){return <div className="portal-content"><CatalogHeading eyebrow="ACCOUNT" title="My Profile" text="Your school account information."/><div className="profile-card"><div className="profile-avatar">{studentId.slice(0,1).toUpperCase()}</div><div><span>Login ID</span><strong>{studentId}</strong></div><div><span>Account type</span><strong>Parent / Student</strong></div><div><span>Access</span><strong>School Store</strong></div></div></div>}

function AdminModuleActive({icon,title,text,onClick}:{icon:React.ReactNode;title:string;text:string;onClick:()=>void}){return <button className="admin-module admin-module-active" onClick={onClick}><div className="feature-icon">{icon}</div><div><strong>{title}</strong><span>{text}</span></div><ArrowRight/></button>}

function AdminPortal({onBack}:{onBack:()=>void}){
  const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[admin,setAdmin]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState('')
  const [schools,setSchools]=useState<SchoolOption[]>([]),[branches,setBranches]=useState<BranchOption[]>([]),[school,setSchool]=useState(''),[branch,setBranch]=useState(''),[tool,setTool]=useState<'menu'|'parent'|'import'|'home'|'schools'|'products'|'packages'|'orders'|'payments'|'inventory'|'students'|'reports'|'catalog'|'coupons'>('menu')
  async function login(){if(!supabase||!email.trim()||!password)return;setLoading(true);setError('');const {data,error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error||!data.user){setError('Invalid admin email or password.');setLoading(false);return}const {data:p,error:pe}=await supabase.from('profiles').select('role').eq('id',data.user.id).single();if(pe||!p||!['admin','super_admin'].includes(p.role)){await supabase.auth.signOut({scope:'local'});setError('This account is not authorized for the Admin Portal.');setLoading(false);return}setAdmin(true);setLoading(false)}
  useEffect(()=>{if(!admin||!supabase)return;void supabase.from('schools').select('id,name').eq('status','active').order('name').then(({data})=>setSchools(data??[]))},[admin])
  async function chooseSchool(v:string){setSchool(v);setBranch('');setBranches([]);if(!v||!supabase)return;const {data}=await supabase.from('branches').select('id,name').eq('school_id',v).eq('status','active').order('name');setBranches(data??[])}
  if(!admin)return <div className="admin-login-page"><div className="admin-login-card"><button className="modal-close" onClick={onBack}><X size={20}/></button><div className="admin-login-logo"><img src="/logo.png" alt="Artisan"/></div><p className="eyebrow">ADMINISTRATION</p><h1>Administrator Sign In</h1><p>Manage schools, students, catalogs and orders from the secure admin portal.</p><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@gmail.com" autoComplete="username"/><label>Password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} onKeyDown={e=>{if(e.key==='Enter')void login()}} autoComplete="current-password"/>{error&&<p className="login-error">{error}</p>}<button className="primary-button" onClick={()=>void login()} disabled={loading||!email||!password}>{loading?'SIGNING IN...':'ADMIN LOGIN'}</button></div></div>
  if(tool==='parent'&&school&&branch)return <ParentCreate schoolId={school} branchId={branch} onBack={()=>setTool('menu')}/>
  if(tool==='import'&&school&&branch)return <StudentImport schoolId={school} branchId={branch} onBack={()=>setTool('menu')}/>
  if(tool==='home')return <HomepageEditor onBack={()=>setTool('menu')}/>
  if(tool==='schools'||tool==='products'||tool==='packages'||tool==='orders'||tool==='payments'||tool==='inventory'||tool==='students'||tool==='reports'||tool==='catalog'||tool==='coupons')return <AdminWorkspace module={tool} onBack={()=>setTool('menu')}/>
  return <div className="admin-shell"><header className="admin-topbar"><div className="site-brand"><img className="brand-logo admin-brand-logo" src="/logo.png" alt="Artisan" /><div><span>Administration</span></div></div><button onClick={()=>{void supabase?.auth.signOut({scope:'local'});setAdmin(false)}}><LogOut size={17}/> Logout</button></header><div className="admin-content"><div className="admin-title"><div><p className="eyebrow">ADMIN DASHBOARD</p><h1>Student & Parent Management</h1><p>Create test accounts individually or import your student register in bulk.</p></div></div><div className="admin-scope-card"><div><span>School</span><strong>{schools.find(x=>x.id===school)?.name??'Select school'}</strong></div><div><span>Branch</span><strong>{branches.find(x=>x.id===branch)?.name??'Select branch'}</strong></div></div><div className="admin-selector-grid"><label>School<select value={school} onChange={e=>void chooseSchool(e.target.value)}><option value="">Select school</option>{schools.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Branch<select value={branch} disabled={!school} onChange={e=>{setBranch(e.target.value);setTool('menu')}}><option value="">Select branch</option>{branches.map(x=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label></div><div className="admin-action-grid"><button onClick={()=>setTool('home')}><Home/><div><strong>Homepage Editor</strong><span>Control hero, sections, images, text and footer.</span></div><ArrowRight/></button><button disabled={!school||!branch} onClick={()=>setTool('parent')}><UserRound/><div><strong>Create Parent Login</strong><span>Create one parent + student account for testing.</span></div><ArrowRight/></button><button disabled={!school||!branch} onClick={()=>setTool('import')}><FileSpreadsheet/><div><strong>Bulk Student Import</strong><span>Upload Excel or CSV student records.</span></div><ArrowRight/></button></div><div className="admin-module-grid"><AdminModuleActive icon={<Building2/>} title="Schools & Branches" text="Manage school campuses and branch details." onClick={()=>setTool('schools')}/><AdminModuleActive icon={<ShoppingBag/>} title="Products & Variants" text="Manage shirts, pants, shoes, sizes and SKUs." onClick={()=>setTool('products')}/><AdminModuleActive icon={<Package/>} title="Uniform Packages" text="Build boys and girls packages from individual products." onClick={()=>setTool('packages')}/><AdminModuleActive icon={<ClipboardList/>} title="Orders" text="Review orders, statuses and customer details." onClick={()=>setTool('orders')}/><AdminModuleActive icon={<CreditCard/>} title="Payments" text="Track payment status and transaction references." onClick={()=>setTool('payments')}/><AdminModuleActive icon={<ClipboardList/>} title="Inventory" text="Monitor stock and inventory movements." onClick={()=>setTool('inventory')}/><AdminModuleActive icon={<UserRound/>} title="Parents & Students" text="Manage student profiles and parent relationships." onClick={()=>setTool('students')}/><AdminModuleActive icon={<FileSpreadsheet/>} title="Reports" text="Sales, inventory and school-level reporting." onClick={()=>setTool('reports')}/><AdminModuleActive icon={<BookOpen/>} title="Catalog Rules" text="Configure school-specific products and pricing." onClick={()=>setTool('catalog')}/><AdminModuleActive icon={<Sparkles/>} title="Coupons" text="Manage branch-specific promotions and discounts." onClick={()=>setTool('coupons')}/></div></div></div>
}

function HomepageEditor({onBack}:{onBack:()=>void}){const [content,setContent]=useState<any>(DEFAULT_HOME),[saving,setSaving]=useState(false),[saved,setSaved]=useState(false),[error,setError]=useState('');useEffect(()=>{if(!supabase)return;void (supabase as any).from('homepage_content').select('content').eq('slug','default').maybeSingle().then(({data,error}:{data:any;error:any})=>{if(error)setError(error.message);else if(data?.content)setContent(data.content)})},[]);const save=async()=>{if(!supabase)return;setSaving(true);setError('');setSaved(false);const {data:user}=await supabase.auth.getUser();const {error:e}=await supabase.from('homepage_content').update({content,updated_by:user.user?.id,updated_at:new Date().toISOString()}).eq('slug','default');if(e)setError(e.message);else setSaved(true);setSaving(false)};const edit=(fn:(c:any)=>void)=>setContent((prev:any)=>{const n=structuredClone(prev);fn(n);return n});return <div className="admin-shell"><header className="admin-topbar"><div className="site-brand"><img className="brand-logo admin-brand-logo" src="/logo.png" alt="Artisan"/><div><span>Homepage Editor</span></div></div><button onClick={onBack}><ArrowLeft size={17}/> Back</button></header><div className="admin-content"><div className="admin-title"><div><p className="eyebrow">HOMEPAGE CMS</p><h1>Control the entire public homepage</h1><p>All homepage copy, imagery URLs, ordering and visibility are stored in Supabase.</p></div><button className="primary-button" onClick={()=>void save()} disabled={saving}>{saving?'SAVING...':'SAVE HOMEPAGE'}</button></div>{saved&&<p className="form-success">Homepage saved successfully.</p>}{error&&<p className="login-error">{error}</p>}<section className="home-edit-section"><h2>Hero Slider</h2>{content.hero.slides.map((s:any,i:number)=><div className="home-edit-card" key={i}><h3>Slide {i+1}</h3><label>Eyebrow<input value={s.eyebrow} onChange={e=>edit(c=>c.hero.slides[i].eyebrow=e.target.value)}/></label><label>Title line 1<input value={s.title[0]} onChange={e=>edit(c=>c.hero.slides[i].title[0]=e.target.value)}/></label><label>Title line 2<input value={s.title[1]} onChange={e=>edit(c=>c.hero.slides[i].title[1]=e.target.value)}/></label><label>Title line 3<input value={s.title[2]} onChange={e=>edit(c=>c.hero.slides[i].title[2]=e.target.value)}/></label><label>Description<textarea value={s.text} onChange={e=>edit(c=>c.hero.slides[i].text=e.target.value)}/></label><label>Image URL<input value={s.image} onChange={e=>edit(c=>c.hero.slides[i].image=e.target.value)}/></label><label>Primary button<input value={s.button} onChange={e=>edit(c=>c.hero.slides[i].button=e.target.value)}/></label><label>Secondary button<input value={s.secondary} onChange={e=>edit(c=>c.hero.slides[i].secondary=e.target.value)}/></label></div>)}</section><section className="home-edit-section"><h2>Trust Strip</h2>{content.trust.map((x:any,i:number)=><div className="home-edit-row" key={i}><label>Title<input value={x.title} onChange={e=>edit(c=>c.trust[i].title=e.target.value)}/></label><label>Text<input value={x.text} onChange={e=>edit(c=>c.trust[i].text=e.target.value)}/></label></div>)}</section><section className="home-edit-section"><h2>Shop by Category</h2>{content.categories.map((x:any,i:number)=><div className="home-edit-row" key={i}><label>Title<input value={x.title} onChange={e=>edit(c=>c.categories[i].title=e.target.value)}/></label><label>Image URL<input value={x.image} onChange={e=>edit(c=>c.categories[i].image=e.target.value)}/></label><label>Target<input value={x.target} onChange={e=>edit(c=>c.categories[i].target=e.target.value)}/></label></div>)}</section><section className="home-edit-section"><h2>Featured Collections</h2>{content.featured.map((x:any,i:number)=><div className="home-edit-card" key={i}><label>Title<input value={x.title} onChange={e=>edit(c=>c.featured[i].title=e.target.value)}/></label><label>Description<textarea value={x.text} onChange={e=>edit(c=>c.featured[i].text=e.target.value)}/></label><label>Image URL<input value={x.image} onChange={e=>edit(c=>c.featured[i].image=e.target.value)}/></label><label>Target<input value={x.target} onChange={e=>edit(c=>c.featured[i].target=e.target.value)}/></label></div>)}</section><section className="home-edit-section"><h2>Lower Sections</h2><label>After-fold eyebrow<input value={content.afterFold.eyebrow} onChange={e=>edit(c=>c.afterFold.eyebrow=e.target.value)}/></label><label>After-fold title line 1<input value={content.afterFold.title[0]} onChange={e=>edit(c=>c.afterFold.title[0]=e.target.value)}/></label><label>After-fold title line 2<input value={content.afterFold.title[1]} onChange={e=>edit(c=>c.afterFold.title[1]=e.target.value)}/></label><label>After-fold text<textarea value={content.afterFold.text} onChange={e=>edit(c=>c.afterFold.text=e.target.value)}/></label><label>After-fold image URL<input value={content.afterFold.image} onChange={e=>edit(c=>c.afterFold.image=e.target.value)}/></label><label>CTA eyebrow<input value={content.cta.eyebrow} onChange={e=>edit(c=>c.cta.eyebrow=e.target.value)}/></label><label>CTA title line 1<input value={content.cta.title[0]} onChange={e=>edit(c=>c.cta.title[0]=e.target.value)}/></label><label>CTA title line 2<input value={content.cta.title[1]} onChange={e=>edit(c=>c.cta.title[1]=e.target.value)}/></label><label>CTA button<input value={content.cta.button} onChange={e=>edit(c=>c.cta.button=e.target.value)}/></label></section><section className="home-edit-section"><h2>Benefits & Footer</h2>{content.benefits.map((x:any,i:number)=><div className="home-edit-row" key={i}><label>Benefit title<input value={x.title} onChange={e=>edit(c=>c.benefits[i].title=e.target.value)}/></label><label>Benefit text<input value={x.text} onChange={e=>edit(c=>c.benefits[i].text=e.target.value)}/></label></div>)}{['tagline','shopTitle','helpTitle','informationTitle','copyright','credit','secondary'].map((k:string)=><label key={k}>{k}<input value={content.footer[k]} onChange={e=>edit(c=>c.footer[k]=e.target.value)}/></label>)}</section></div></div>}

export default App
